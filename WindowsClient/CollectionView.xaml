<UserControl x:Class="Overmind.ImageManager.WindowsClient.CollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:main="clr-namespace:Overmind.ImageManager.WindowsClient"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance Type=main:CollectionViewModel}"
             d:DesignHeight="300" d:DesignWidth="700">

    <UserControl.Style>
        <Style TargetType="UserControl">
            <Style.Triggers>
                <Trigger Property="DataContext" Value="{x:Null}">
                    <Setter Property="IsEnabled" Value="False"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>
        <ListView Grid.Column="0" ItemsSource="{Binding Path=ImageCollection}"
                  SelectedItem="{Binding Path=SelectedImage}" Margin="5">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Column="1">
            <DockPanel Margin="5">
                <Button Content="Add" Command="{Binding Path=AddImageCommand}"
                        CommandParameter="{Binding ElementName=newImageUri, Path=Text}"
                        DockPanel.Dock="Right" Margin="5" Width="50"/>
                <TextBox x:Name="newImageUri" x:FieldModifier="private"
                         DockPanel.Dock="Left" Margin="5"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</UserControl>
