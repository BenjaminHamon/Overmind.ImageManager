<UserControl x:Class="Overmind.ImageManager.WindowsClient.ImagePropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:globalization="clr-namespace:System.Globalization;assembly=mscorlib"
             xmlns:main="clr-namespace:Overmind.ImageManager.WindowsClient"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance Type=main:ImagePropertiesViewModel}"
             d:DesignHeight="400" d:DesignWidth="300">
    
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsReadOnly" Value="True">
                    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="main:TokenListView">
            <Style.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="MinWidth" Value="20"/>
                </Style>
            </Style.Resources>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Title"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Title}"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Subjects"/>
            <main:TokenListView Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Path=SubjectCollection}" Margin="5" MinHeight="30">
                <main:TokenListView.TextBoxTemplate>
                    <DataTemplate>
                        <main:AutocompleteTextBox d:DataContext="{d:DesignInstance Type=main:ObservableString}" Text="{Binding Path=Value}">
                            <main:AutocompleteTextBox.AllValues>
                                <Binding RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=main:TokenListView}" Path="DataContext.AllSubjects"/>
                            </main:AutocompleteTextBox.AllValues>
                        </main:AutocompleteTextBox>
                    </DataTemplate>
                </main:TokenListView.TextBoxTemplate>
            </main:TokenListView>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Artists"/>
            <main:TokenListView Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Path=ArtistCollection}" Margin="5" MinHeight="30">
                <main:TokenListView.TextBoxTemplate>
                    <DataTemplate>
                        <main:AutocompleteTextBox d:DataContext="{d:DesignInstance Type=main:ObservableString}" Text="{Binding Path=Value}">
                            <main:AutocompleteTextBox.AllValues>
                                <Binding RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=main:TokenListView}" Path="DataContext.AllArtists"/>
                            </main:AutocompleteTextBox.AllValues>
                        </main:AutocompleteTextBox>
                    </DataTemplate>
                </main:TokenListView.TextBoxTemplate>
            </main:TokenListView>
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Tags"/>
            <main:TokenListView Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Path=TagCollection}" Margin="5" MinHeight="30">
                <main:TokenListView.TextBoxTemplate>
                    <DataTemplate>
                        <main:AutocompleteTextBox d:DataContext="{d:DesignInstance Type=main:ObservableString}" Text="{Binding Path=Value}">
                            <main:AutocompleteTextBox.AllValues>
                                <Binding RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=main:TokenListView}" Path="DataContext.AllTags"/>
                            </main:AutocompleteTextBox.AllValues>
                        </main:AutocompleteTextBox>
                    </DataTemplate>
                </main:TokenListView.TextBoxTemplate>
            </main:TokenListView>
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Score"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Path=Score}"/>
            <TextBlock Grid.Row="5" Grid.Column="0" Text="Added on"/>
            <TextBox Grid.Row="5" Grid.Column="1" IsReadOnly="True">
                <Binding Path="AdditionDate" Mode="OneWay" StringFormat="g"
                         ConverterCulture="{x:Static globalization:CultureInfo.CurrentCulture}"/>
            </TextBox>
            <TextBlock Grid.Row="6" Grid.Column="0" Text="Source"/>
            <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Path=Source}" TextWrapping="Wrap"/>
            <TextBlock Grid.Row="7" Grid.Column="0" Text="Hash"/>
            <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding Path=Hash, Mode=OneWay}" IsReadOnly="True"/>
            
        </Grid>
    </ScrollViewer>
</UserControl>
